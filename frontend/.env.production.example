# Variables de build del frontend en producción (Cloudflare Pages).
# Copiar a .env.production (o definir en el dashboard de Pages como variable de
# build). Debe apuntar a la URL pública del backend en Render.
NEXT_PUBLIC_API_URL=https://micelio-api.onrender.com
